<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="18dp"
    android:paddingVertical="8dp">

    <ImageView
        android:id="@+id/iv_search_pet_profile_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/shp_circle_image"
        android:clipToOutline="true"
        android:contentDescription="@string/label_pet_profile_image"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/img_pomeranian_01" />

    <TextView
        android:id="@+id/tv_search_pet_name"
        style="@style/AppTextTitle.S18"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_search_pet_profile_image"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="누렁이 | 서정우" />

    <TextView
        android:id="@+id/tv_search_phone_number"
        style="@style/AppTextLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/tv_search_pet_name"
        app:layout_constraintTop_toBottomOf="@id/tv_search_pet_name"
        tools:text="010-1234-5678" />

</androidx.constraintlayout.widget.ConstraintLayout>